apiVersion: v1
kind: ServiceAccount
metadata:
  name: aws-tag-exporter
  labels:
    app: aws-tag-exporter
  annotations:
    # For AWS IRSA (IAM Roles for Service Accounts)
    # Replace with your IAM role ARN
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/aws-tag-exporter
